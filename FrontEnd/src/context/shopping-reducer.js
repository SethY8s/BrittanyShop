import { ADD_TO_CART, REMOVE_FROM_CART, LOAD_CURRENT_ITEM, ADJUST_QTY} from './shopping-types'

const shoppingReducer = (state, action) => {
    switch(action.type) {

    }
}

export default shoppingReducer